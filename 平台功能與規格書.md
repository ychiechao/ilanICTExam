# 宜蘭縣資訊科技創意實作競賽平台 功能與軟體規格書（現況版）

| 項目 | 內容 |
| --- | --- |
| 版本 | v3.0 現況版（依 2026-09-22 正式站程式整理） |
| 正式站 | https://ilanictexam.pages.dev（GitHub `main`） |
| 預覽站 | https://v3-dev.ilanictexam.pages.dev（GitHub `v3-dev`） |
| 程式碼 | https://github.com/ychiechao/ilanICTExam |
| 前端 | React 19、TypeScript、Vite 7、Blockly 12（Scratch／Blockly 兩種積木外觀） |
| 後端 | Firebase Authentication、Cloud Firestore、Cloudflare Worker `ilanictexam-grader` + KV |
| 部署 | Cloudflare Pages（前端）、Cloudflare Workers（評分與賽事 API）、Firebase（規則與索引） |
| 相關文件 | `software-spec-v3.md`（規劃過程與第 17 章實作差異）、`implementation-plan.md`、`README.md`、使用說明書（Claude 文件）、教師簡報 `教師使用說明.pptx` |

本文件是「目前系統長什麼樣」的單一參考：第 1–3 章是給主辦單位與老師看的功能總覽，第 4 章以後是技術規格。

---

## 1. 系統定位

一套積木程式解題平台，用「輸入→輸出」的題目自動評分，有兩種運作模式，由主辦單位（超級管理者）在後台一鍵切換，所有已登入使用者的畫面即時跟隨：

| 模式 | 用途 | 誰能登入 |
| --- | --- | --- |
| 練習模式（平時） | 老師開班帶學生練習，班級／學校／全縣排行 | 學生、教師、超管（Google 帳號） |
| 競賽模式（賽事當天） | 國小組、國中組兩場並行，參賽者用印製帳號作答，伺服器端評分 | 參賽者（競賽帳號）、超管 |
| 維護模式 | 全站只顯示公告 | 超管 |

另有 **演練賽**：平台維持練習模式，額外開放指定賽事讓測試帳號登入作答，用於賽前一週讓學生熟悉環境。

## 2. 使用者與權限

| 角色 | 登入方式 | 啟用條件 | 能做什麼 |
| --- | --- | --- | --- |
| 學生 | Google 帳號 | 設定學校（自選或加入班級帶入）後成績才記錄 | 解題、自行測試、評分、評分紀錄、三層排行榜、加入班級 |
| 教師 | Google 帳號（學校教師信箱） | 超管在使用者管理設定教師身分與任教學校 | 開班、班級代碼、改名／封存、學生名單、答題儀表板、進度矩陣、匯出 CSV、114 解題手冊；也能解題 |
| 超級管理者（主辦單位） | Google 帳號（在 `admins` 名單） | — | 平台模式、學校、使用者、練習題庫、賽事、競賽帳號、競賽題庫、儀表板、成績審核、稽核 |
| 參賽者 | 賽事帳號（E-001／J-001）＋密碼 | 該場賽事開放中（競賽模式啟用或演練勾選） | 作答、提交、看自己的評分紀錄、（開放時）排行榜 |

規則：未設定學校的學生或教師只能線上測驗，成績不記錄、不進排行榜；學生一旦加入班級，學校以班級為準且不可再自選；競賽帳號永遠碰不到練習資料，教師學生在競賽模式下只看到公告。

## 3. 功能總覽

### 3.1 練習模式（學生）

| 功能 | 說明 |
| --- | --- |
| 題目列表 | 依年度、分類篩選；卡片顯示未作答／未完成／已完成 |
| 題目說明 | 題目、輸入輸出格式、公開範例；隱藏測資計分但不顯示 |
| 積木工作區 | Scratch 模式（綠旗、詢問、說出）與 Blockly 模式（要求輸入、輸出）可切換，積木自動互換；草稿存瀏覽器；下載／上傳 XML；重設 |
| 執行程式／自行測試 | 在瀏覽器執行，自訂輸入，不計分 |
| 評分 | 全部測資（含隱藏）在瀏覽器計分，寫入提交紀錄、每人每題統計、班級答題紀錄、排行榜彙總（教師／超管不寫彙總） |
| 評分紀錄 | 本題每次提交分數、個人練習總覽（已完成／練習中／未完成） |
| 排行榜 | 班級／學校／全縣三個分頁，依答題率 → 完成題數 → 總分排序；顯示自己的名次；點選展開學校、姓名。**只列學生**：教師與超管解題仍有個人紀錄，但不寫入排行榜彙總 |
| 我的帳號 | 輸入班級代碼加入班級（學校自動帶入）、未加入前可自選學校、看班級與教師身分狀態 |

### 3.2 練習模式（教師：我的班級）

| 分頁 | 功能 |
| --- | --- |
| 班級管理 | 建立班級（自動產生代碼 ILC-xxxx）、開放／關閉加入、改名、封存／取消封存（封存後關閉加入、不出現在排行榜，紀錄保留）、看答題記錄、114 解題手冊連結 |
| 學生名單 | 依班級或全部；點選學生展開個人答題儀表板；移除／恢復學生 |
| 答題記錄 | 班級儀表板（學生數、已作答、提交次數、平均答題率）、各題完成狀況、最近提交 50 筆；匯出「學生進度 CSV」（一人一列、每題一欄）與「作答紀錄 CSV」（每筆提交一列） |
| 進度矩陣 | 學生×題目：✓ 已完成、百分比為最佳答題率、空白未作答；可篩年度、分類、只列有人作答的題目；點格子看該生該題每次提交；匯出 CSV |

### 3.3 後台（超級管理者）

| 頁籤 | 功能 |
| --- | --- |
| 平台狀態 | 練習／競賽／維護模式切換（切競賽模式前檢查賽事狀態、帳號、題庫、比賽長度）；競賽模式啟用賽事多選；演練賽事多選；全站公告；比賽控制（開始／暫停／繼續／結束，永遠列出所有進行中的賽事） |
| 儀表板 | 選賽事；帳號數、目前線上、登入過、離開考試畫面中、離開次數、已提交、提交次數、平均總分；即時排行、各題完成、各校統計、最近提交；對外顯示（只有主辦單位／開放給參賽者／開放給所有登入者）、顯示姓名、投影畫面連結（不需登入、每 10 秒更新、可重新產生使舊連結失效） |
| 成績審核 | 載入該場全部提交；依參賽者彙總並標記異常（同題程式碼與他人相同、結束前 60 秒提交、離開考試畫面 ≥ 3 次、貼上、登入裝置變更）；篩選、搜尋；展開看每筆提交與程式碼；作廢（填原因）／恢復，立即重算排行；匯出成績總表、提交明細 CSV |
| 賽事管理 | 新增草稿（年度、名稱、組別 E／J、比賽長度、每題提交上限、預定時段、說明、備註）；階段推進／退回；匯入帳號／題庫捷徑；釋出到練習題庫；封存／解封存；重置；刪除（空草稿）；複製明年 |
| 競賽帳號 | 貼「學校,姓名」名單 → 預覽 → 匯入；系統產生帳號、密碼、各校序號；密碼只顯示一次：下載 CSV 或列印 A4 帳號卡（QR 含帳號）；帳號清單、重設密碼、停用／啟用；學校自動對應或自動新增 |
| 競賽題庫 | 選 bDesigner JSON → 預覽 → 匯入（整份取代）；題目說明進資料庫、測資進 Worker KV；進行中不可換題庫 |
| 學校管理 | 新增／編輯學校、Email 網域、啟用／停用 |
| 題目管理 | 練習題庫匯入（bDesigner JSON／CSV，覆蓋或附加）、編輯（年度、分類、說明、測資、公開／隱藏、狀態 published／draft／archived）、匯出 CSV 與範本；有作答紀錄的題目不可刪除 |
| 使用者管理 | 依身分、學校篩選；設定所屬學校、教師身分、超管、停用、刪除（連同紀錄與排行）；「未設學校」快速找出 |
| 使用者解題資料 | 全站每人完成題數、答題率、最後作答；展開各題最佳成績；重建排行榜彙總 |

### 3.4 競賽模式（參賽者）

1. 首頁只顯示公告與「參賽者登入」（演練賽則是練習模式首頁右上「模擬賽登入」）。
2. 登入後「等待開始」；主辦按開始後自動出現倒數（超過一天顯示天數）與題目。
3. 「進入考試」進全螢幕；離開全螢幕會蓋上遮罩，回到全螢幕才能繼續；切分頁、視窗失焦、貼上都記錄。
4. 作答區：左題目清單、中積木（Scratch／Blockly）、右題目說明／自行測試／評分／評分紀錄／（開放時）排行榜。「執行程式」在本機不計分；「提交」送 Worker 評分，每題上限次數（預設 10）、兩次間隔 5 秒；成績以每題最後一次提交為準。
5. 評分紀錄：各題狀態、本題每次結果、看結果、載入該次積木。
6. 暫停時顯示暫停畫面且不計時；結束或時間到後不能再提交。

### 3.5 賽事生命週期

```
草稿 → 報名／名單匯入 → 等候開始 → 競賽中 ⇄ 暫停 → 結束 → 成績審核 → 正式公布 → 封存
```

- 草稿～等候開始、結束～封存可用「上一階段」退回；報名 → 等候開始須已匯入帳號、題庫並設定比賽長度。
- 開始／暫停／繼續／結束只在「平台狀態 → 比賽控制」；切回練習模式時仍在競賽中的賽事自動結束。
- 封存：停用該場全部帳號並記住原階段；解封存：回到原階段並恢復帳號。
- 重置：清掉帳號、題庫、作答、排行、事件、快照與 KV，保留設定、退回草稿。刪除：只限沒有任何資料的草稿。
- 釋出：把該場題目連同隱藏測資複製成練習題庫草稿題（已存在的略過）。

### 3.6 主辦單位作業流程

| 時間 | 動作 |
| --- | --- |
| 賽前數週 | 學校管理建好全縣學校；新增國小組、國中組兩場賽事；匯入題庫；匯入名單、列印帳號卡；推到「等候開始」 |
| 賽前一週（選用） | 建「模擬賽」（長度可到 14 天、提交上限 30–50），匯入測試帳號，平台狀態勾「演練賽事」並開始；結束後取消勾選、重置 |
| 比賽當天 | 平台模式 → 競賽模式，勾兩場、填公告 → 儀表板確認線上人數 → 比賽控制「開始」→ 突發用「暫停／繼續」→ 時間到自動結束 → 平台模式切回練習 |
| 賽後 | 成績審核（異常、作廢、匯出）→ 正式公布 → 釋出題庫到練習 → 封存 → 複製明年 |

---

## 4. 系統架構

```
瀏覽器（React + Blockly）
  ├─ Firebase Auth：Google 登入（教師、學生、超管）／自訂 token（競賽帳號）
  ├─ Firestore：直接讀寫練習資料（受 Rules 保護）、訂閱平台模式、賽事、儀表板
  └─ Cloudflare Worker ilanictexam-grader（HTTPS JSON）
        ├─ 競賽登入、換發 token（服務帳號簽發自訂 token）
        ├─ 競賽評分（JS-Interpreter 逐步執行，測資只在 KV）
        ├─ 帳號／題庫匯入、賽事重置／刪除／封存／釋出、作廢
        └─ 投影畫面 /board
Cloudflare Pages：正式 main、預覽 v3-dev（同一 Firebase 專案）
```

- 練習模式的評分在瀏覽器執行（`shared/grading.ts` 與 Worker 共用比對邏輯）；競賽模式的評分、寫入全部由 Worker 執行，前端只能讀。
- 伺服器時間：`/time` 校正時鐘，倒數與提交時段以伺服器時間為準。
- 平台狀態 `settings/platform` 由所有人訂閱（onSnapshot），切換即時生效。

### 4.1 評分引擎與限制

| 項目 | 練習（瀏覽器） | 競賽（Worker） |
| --- | --- | --- |
| 執行方式 | Web Worker 執行產生的 JavaScript | JS-Interpreter（Workers 禁止 eval／new Function） |
| 輸入／輸出 | `prompt` 依序餵測資、`console.log`／`alert` 收集輸出 | 同左，以原生函式注入 |
| 預算 | — | 每筆測資 20,000 步、每次提交 60,000 步、輸出 1,000 字；超出判該筆錯誤 |
| 比對 | 逐行標準化後比對（去尾空白、統一換行） | 同左 |
| 提交限制 | 無 | 每題上限（賽事設定 1–50，預設 10）、同帳號 5 秒一次 |
| 成績採計 | 每題最佳成績 | 每題最後一次未作廢的提交 |

### 4.2 Worker 路由

| 方法與路徑 | 用途 | 權限 |
| --- | --- | --- |
| GET `/time` | 伺服器時間 | 公開 |
| POST `/login` | 競賽帳號登入 → 自訂 token（每帳號每分鐘 10 次） | 競賽帳號 |
| POST `/refresh` | 換發 token | 競賽帳號 |
| POST `/grade` | 評分並寫入提交、排行、儀表板快照（最多每 3 秒重算） | 競賽帳號（該場開放中、進行中） |
| POST `/contest-accounts/{id}` | 匯入帳號（學校,姓名） | 超管 |
| POST `/contest-accounts/{id}/reset-password` | 重設密碼 | 超管 |
| POST `/contest-accounts/{id}/status` | 停用／啟用 | 超管 |
| POST `/contest-problems/{id}` | 匯入題庫（整份取代） | 超管 |
| GET `/board/{id}?token=&format=json` | 投影畫面 HTML／JSON | 持 token |
| POST `/contests/{id}/reset` | 重置 | 超管 |
| DELETE `/contests/{id}` | 刪除空草稿 | 超管 |
| POST `/contests/{id}/archive`、`/unarchive` | 封存、解封存（連同帳號停用／恢復） | 超管 |
| POST `/contests/{id}/release` | 釋出題庫到練習題庫 | 超管 |
| POST `/contests/{id}/void` | 作廢／恢復單筆提交並重算 | 超管 |

「該場開放中」＝ 平台為競賽模式且在 `activeContestIds`，或在 `rehearsalContestIds`。所有超管操作都寫 `auditLogs`。

### 4.3 環境與部署

| 項目 | 內容 |
| --- | --- |
| Worker 設定 `worker/wrangler.jsonc` | `FIREBASE_PROJECT_ID`、`ALLOWED_ORIGINS`（含兩個站網址）、KV `CONTEST_CASES`、`PASSWORDS`；密鑰 `FIREBASE_SERVICE_ACCOUNT_B64` |
| 前端環境 | `.env.local`：Firebase Web 設定、`VITE_GRADER_URL=http://127.0.0.1:8787`；`.env.production`：正式 Worker 網址 |
| 部署指令 | Worker：`npx wrangler deploy --config worker/wrangler.jsonc`；前端：`npx vite build` → `npx wrangler pages deploy dist --project-name ilanictexam --branch main`（正式）／`--branch v3-dev`（預覽）；規則與索引：`npx firebase deploy --only firestore` |
| 注意 | 兩站共用同一 Firebase 專案，規則與索引一部署即同時生效；Windows PowerShell 5.1 用 `npx.cmd`，一行一指令 |
| 免費額度 | Firestore 每日 5 萬讀／2 萬寫；Workers 每日 10 萬次、每次 CPU 10 ms；線上心跳每 5 分鐘一次以節省寫入。縣賽規模（兩場各 200 人、2 小時）估計在額度內，賽前建議壓力測試 |

---

## 5. 資料模型（Firestore）

| 集合 | 文件 ID | 主要欄位 | 寫入者 |
| --- | --- | --- | --- |
| `settings/platform` | 固定 | `mode`、`activeContestIds[]`、`rehearsalContestIds[]`、`announcement`、`updatedAt/By` | 超管 |
| `users` | uid | `displayName`、`email`、`role`、`schoolId/Name`、`disabled` | 本人／超管 |
| `admins` | uid | `role`（super／teacher）、`schoolId`、`schoolIds[]`、`status` | 超管 |
| `schools` | id | `name`、`emailDomain`、`enabled`、備註 | 超管 |
| `problems` | 題目 ID | 題目說明、`cases[]`（含 visibility）、`year`、`category`、`status`、`source`、`sourceContestId` | 超管、Worker（釋出） |
| `submissions` | 自動 | 練習提交：使用者、題目、分數、測資結果、積木 XML、程式碼、時間 | 本人 |
| `userProblemStats` | `{uid}_{problemId}` | 每人每題最佳分數、答題率、次數、是否完成 | 本人 |
| `userStats` | uid | 排行榜彙總：總分、完成題數、答題率、`schoolId`、`classIds[]`、最後提交 | 本人／超管回填 |
| `classes` | id | `name`、`schoolId/Name`、`teacherUid/Name`、`joinCode`、`joinEnabled`、`archived` | 教師 |
| `classMembers` | id | `classId`、`className`、`studentUid/Name/Email`、`status`、`classArchived` | 學生（加入）、教師 |
| `classSubmissionViews` | id | 學生加入班級後每筆提交的班級視角副本 | 本人 |
| `contests` | id | 年度、名稱、`division`、`status`、`startAt/endAt/pausedAt`、`durationMinutes`、`maxSubmissionsPerProblem`、`accountCount`、`problemCount`、`dashboard{visibility,showNames,topN,boardToken}`、`archivedFromStatus`、`releasedToPractice` | 超管、Worker |
| `contestAccounts` | `{contestId}_{username}` | 姓名、學校、`schoolSeq`、`status`、`disabledReason`、首次／最後登入、`deviceFingerprint` | Worker |
| `contestProblems` | `{contestId}_{problemId}` | 題目公開部分、`caseCount`、`publicCaseCount`、`maxScore`（無隱藏測資） | Worker |
| `contestSubmissions` | `{contestId}_{username}_{problemId}_{ts}` | 分數、測資結果（隱藏只留結果）、積木、程式碼、`codeHash`、`attempt`、`voided`、作廢原因 | Worker |
| `contestLeaderboards` | `{contestId}_{username}` | 總分、完成數、`bestByProblem`（每題最後一次）、`submitCount` | Worker |
| `contestDashboards` | contestId | 快照：排行前 100、各題、各校、`computedAtMs` | Worker |
| `contestPresence` | `{contestId}_{username}` | 心跳時間、`inFullscreen`、`leaveCount` | 參賽者 |
| `contestEvents` | 自動 | `type`（fullscreen_exit、tab_hidden、window_blur、paste、fingerprint_changed…）、detail | 參賽者、Worker |
| `auditLogs` | 自動 | `action`、目標、摘要、操作者 | 超管、Worker |

Worker KV：`cases:{contestId}:{problemId}`（完整測資）、`pw:{contestId}_{username}`（PBKDF2 密碼雜湊）。

複合索引：`contestSubmissions(contestId, createdAt desc)`；`userStats(passRate desc, completedCount desc, totalScore desc)` 及加 `schoolId`、`classIds` 前綴的兩組。

## 6. 安全與規則要點

- 練習資料只在練習模式對教師與學生開放（`practiceOpen()`），競賽帳號一律拒絕；沒有學校的帳號不能寫提交、統計、排行。
- 競賽帳號的身分來自 Worker 簽發的自訂 token claims（`accountType`、`contestId`、`username`），只能讀自己那一場的賽事、題目（公開部分）、自己的提交；寫入只有心跳與事件。
- `contestSubmissions`、`contestLeaderboards`、`contestDashboards`、`contestAccounts`、`contestProblems` 前端不可寫；儀表板與排行對外可見範圍依 `contests.dashboard.visibility`。
- 密碼只存 PBKDF2 雜湊（100,000 次），帳號不存在與密碼錯誤回同一訊息；登入限速。
- 隱藏測資只在 KV，前端任何路徑拿不到；提交程式碼與 `codeHash` 保留供審核。
- 全螢幕軟鎖只記錄與嚇阻，無法真正阻止切出；現場建議 Chrome Kiosk 模式。

## 7. 資料格式

| 檔案 | 格式 |
| --- | --- |
| 題庫（練習與競賽共用） | bDesigner 匯出的 JSON：每題 `id`、`title`、`description`、`inputFormat`、`outputFormat`、`examples[]`、`cases[]`（`input`、`output`、`score`、`visibility`）、`category`、`difficulty` |
| 練習題庫 CSV | 後台可下載範本；覆蓋或附加 |
| 競賽帳號名單 | 每列「學校,姓名[,備註]」，逗號或 Tab 分隔，標題列自動略過 |
| 帳號密碼 CSV（匯入當下） | 學校序號、學校、姓名、帳號、密碼 |
| 帳號清單 CSV | 學校序號、學校、姓名、帳號、狀態、首次登入 |
| 成績總表 CSV | 名次、學校序號、學校、姓名、帳號、總分、完成題數、有效／作廢提交、離開次數、異常標記、各題分數 |
| 提交明細 CSV | 學校、姓名、帳號、題目、第幾次、時間、分數、滿分、通過測資、狀態、程式碼雜湊、相同程式碼者、結束前 60 秒、作廢、原因 |
| 班級 CSV | 學生進度（一人一列每題一欄）、作答紀錄（每筆一列）、進度矩陣 |

## 8. 尚未實作與延伸

| 項目 | 狀態 |
| --- | --- |
| 教師批次登記學生（貼 Email 名單預建、學生登入自動認領） | 未做（計畫 4.5–4.6） |
| Google Classroom 匯入名單 | 待確認 GCP 專案可設「內部」OAuth、縣網開放 Classroom API |
| 賽前壓力測試（兩場各 200 人同時提交） | 未做 |
| 每人計時（登入起算 2 小時） | 未做，演練週可用整場 2 小時模擬正式賽替代 |
| 教師觀賽、單題排行、PK／徽章 | 延伸 |
| 積木作品檢核點（App Inventor `.aia`／Scratch `.sb3` 依範例檢核計分） | 獨立 PoC（`poc-rubric/`），未整合 |
| 真正 Scratch 3 編輯器與 `.sb3` 自動評分 | 評估中：需 iframe 嵌入 scratch-gui、競賽評分需另建 Node 服務；同一 Firebase 專案可共用師生與班級，用 `engine` 欄位區分 |
